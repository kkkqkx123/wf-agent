# LLM重试策略配置
# 从global.toml迁移而来

# 全局重试配置
[retry_config]
base_delay = 1.0
max_delay = 60.0
jitter = true
exponential_base = 2.0
retry_on_status_codes = [429, 500, 502, 503, 504]

# 需要重试的错误类型
[retry_config.retry_errors]
types = [
  "timeout",
  "rate_limit",
  "service_unavailable"
]