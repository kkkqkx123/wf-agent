# Task Store

This directory is reserved for future workflow execution storage implementations.

In the new architecture, WorkflowExecutionRegistry directly interfaces with the Storage layer,
eliminating the need for a separate TaskRegistry abstraction.

See WorkflowExecutionRegistry for the current implementation.
