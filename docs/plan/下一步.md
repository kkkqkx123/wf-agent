过程处理：
- 拒绝
这是最简单的选项，它会拒绝任何后续运行，并且不允许重复发送消息。请参阅操作指南，了解如何配置拒绝重复发送消息选项。

- 排队
这是一个相对简单的选项，它会继续第一次运行直到完成整个运行，然后将新输入作为单独的运行发送。请参阅操作指南，了解如何配置排队重复发送消息选项。

- 中断
此选项会中断当前执行，但会保存截至该点的所有已完成工作。然后它会插入用户输入并从那里继续。

如果启用此选项，您的图应该能够处理可能出现的奇怪边缘情况。例如，您可能已经调用了一个工具，但尚未从该工具的运行中获得结果。您可能需要删除该工具调用，以避免出现悬空工具调用。

- 回滚
此选项会中断当前执行并回滚截至该点的所有已完成工作，包括原始运行输入。然后它会发送新的用户输入，基本上就像它是原始输入一样。



wait节点：等待事件触发后继续执行？是否需要与触发器协调？


触发子工作流新增逻辑：支持上下文处理，这样可以避免完整拷贝上下文后再使用上下文处理，导致为了可恢复性增加额外的存储占用


？消息归档？

cli-apps重构