似乎根本不应该提供syncToHostFS选项，永远都应该同步。因为vfs的目的在于拦截危险操作，例如文件编辑工具改到了工作路径外的某个配置文件、shell工具错误使用rm -rf \*等，这些一旦出现就需要拒绝+给出说明/立刻切换到人工审核(可以复用approval模块的功能)。vfs应该是针对某个操作同步阻塞的，只要通过了检查就马上同步回去(不过需要保证不会因为并发问题导致假成功，是否成功依然要根据实际情况)，而非作为中间状态。所有没有被拦截的操作都应该是与真实文件完全同步的
