/spec 
分析当前项目如果要在apps目录实现cli app，需要如何实现。仅使用api层提供的功能。如果api层提供的功能不足，可以给出api层的扩展方案。
注意：仅仅是实现用于实现基本功能，提供简单的终端命令操作，不需要任何形式的TUI。可以采用新建终端的方式来执行任务，保证初始的终端始终可以接收指令。

主要功能包括：
注册工作流、节点模板、工作流模板
基本的crub操作


提供cli-api接口，方便llm操作