/**
 * Command Safety — Shared Utilities
 *
 * Provides command chain parsing and other safety utilities shared between
 * auto-approval (pre-execution decision layer) and sandbox (runtime enforcement layer).
 */

export { parseCommandChain } from "./command-chain-parser.js";
