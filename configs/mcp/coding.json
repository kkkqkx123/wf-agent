{
  "id": "coding",
  "name": "Coding-Specific MCP Servers",
  "description": "MCP servers focused on coding assistance (language servers, linters, etc.)",
  "mcpServers": {
    "example-language-server": {
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server- languageserver", "--stdio"],
      "disabled": true,
      "timeout": 120,
      "lifecycle": "lazy",
      "idleTimeout": 600,
      "healthCheckInterval": 60
    }
  }
}