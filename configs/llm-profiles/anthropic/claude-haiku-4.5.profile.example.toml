# Claude Haiku 4.5 — Fast & Cheap
#
# Anthropic's fastest and most cost-effective model.
# Ideal for high-volume classification, moderation, and simple extraction.

id = "claude-haiku-4.5"
name = "Claude Haiku 4.5"
provider = "ANTHROPIC"
model = "claude-haiku-4.5"
apiKey = "${ANTHROPIC_API_KEY}"
baseUrl = "https://api.anthropic.com/v1"
timeout = 30000
maxRetries = 2
retryDelay = 1000

[parameters]
temperature = 0.3
maxTokens = 8192

[metadata]
tags = ["anthropic", "claude-haiku", "fast", "cheap"]