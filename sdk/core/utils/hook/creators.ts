/**
 * Hook Creator Tool (General Part)
 * (Deprecated - hook-level custom handler execution has been removed)
 *
 * Hook-level custom handler functions embedded in eventPayload["handler"]
 * are no longer automatically executed. Use Trigger Custom Handler instead.
 */

// No exports remain — all hook creator functions have been removed.
// The Trigger Custom Handler system should be used for custom logic.
export {};