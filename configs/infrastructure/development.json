{
  "id": "development",
  "name": "Development Infrastructure",
  "description": "Infrastructure configuration for development environment",
  "tags": ["dev"],
  "files": {
    "metrics": "./metrics.toml",
    "timeout": "./timeout.toml",
    "storage": "./storage.toml",
    "output": "./output.toml",
    "file_checkpoint": "./file-checkpoint.toml",
    "presets": "./presets.toml",
    "tools": "./tools.toml",
    "sandbox": "./sandbox.toml",
    "tool_approval": "./tool-approval.toml"
  }
}
