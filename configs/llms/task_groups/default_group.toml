# 默认任务组配置
name = "default_group"

[echelon1]
model = "gpt-4o-mini"
provider = "openai"
priority = 1

[echelon2]
model = "gpt-5-mini"
provider = "openai"
priority = 2

[circuit_breaker]
failure_threshold = 8
reset_timeout = 90